@model IEnumerable<ProjetFinal.Models.ReglageConfig>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Theme)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NomMembre)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.LangPref)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NotificationAct)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Theme)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NomMembre)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LangPref)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NotificationAct)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdConfig">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdConfig">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdConfig">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
